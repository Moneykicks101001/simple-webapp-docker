name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  # build:
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Code checkout 
  #     uses: actions/checkout@v2

  #   - name: Install Google Cloud SDK
  #     uses: google-github-actions/setup-gcloud@v0
  #     with:
  #       project_id: ${{ secrets.PROJECT_ID }}  
  #       service_account_key: ${{ secrets.GCP_KEY }}  

  #   - name: Build and Push the Docker image
  #     env: 
  #       Project_id: ${{ secrets.PROJECT_ID }}
  #       TAG: "${{ github.run_number }}"
  #     run: |
  #       gcloud auth configure-docker us-central1-docker.pkg.dev
  #       docker build -t us-central1-docker.pkg.dev/$Project_id/demo-dev-repository-us-central1/simpython .
  #       docker push us-central1-docker.pkg.dev/$Project_id/demo-dev-repository-us-central1/simpython


  deploy:
    #needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Install Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v0
      with:
        project_id: ${{ secrets.PROJECT_ID }}  
        service_account_key: ${{ secrets.GCP_KEY }}  

    - name: Google Auth
      id: auth
      uses: google-github-actions/auth@v0
      with: 
        credentials_json: ${{ secrets.GCP_KEY }}  

    - name: Deploy to Cloud Run
      env: 
        Project_id: ${{ secrets.PROJECT_ID }}
        TAG: "${{ github.run_number }}"
      uses: google-github-actions/deploy-cloudrun@main
      with:
        image: us-central1-docker.pkg.dev/$Project_id/demo-dev-repository-us-central1/simpython@sha256:69e2a1d4cde410ab88c56e0ea6e576ef8e5d2ac34d2897739c21f68544a6e6d1
        service: simpython
        region: us-central1