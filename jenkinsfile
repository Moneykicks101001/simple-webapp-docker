pipeline {
    agent any

    stages {
        stage('Step 1: Say Hello') {
            steps {
                echo 'Hello'
            }
        }

        stage('Step 2: start server') {
            steps {
                sh 'python3 app.py'
            }
        }
    }
}
